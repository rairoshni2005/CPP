#include <iostream>
using namespace std; 

int main() {
    // Define an array of characters
    char str[] = "Hello Areeb!";
    
    // Get the length using strlen function from cstring
    int length = strlen(str);
    
    // Print the length
    cout << "The length of the string is: " << length << endl;

    return 0;
}
